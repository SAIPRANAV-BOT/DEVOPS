name: CI Minimal

on:
  push:
    branches:
      - main
      - main-branch
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Say hello (dummy step)
        run: echo "CI workflow is running fine ✅"